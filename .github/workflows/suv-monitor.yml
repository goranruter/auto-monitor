name: suv-monitor
on:
  schedule:
    - cron: '0 5-20 * * *' # every hour at :00, 07:00–22:00 Belgrade (UTC+2)
  workflow_dispatch:

jobs:
  monitor:
    runs-on: self-hosted
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: true

      - name: Install dependencies
        run: npm ci && npx playwright install chromium

      - name: Run SUV monitor
        run: node scripts/monitor.js
        env:
          MONITOR_TYPE: suv
          NOTIFY_EMAIL: ${{ secrets.NOTIFY_EMAIL_SUV }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}

      - name: Commit seen_suv.json if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git stash || true
          git pull --rebase || true
          git stash pop || true
          git add seen_suv.json || true
          if git diff --staged --quiet; then
            echo "no changes to commit"
          else
            git commit -m "chore: update seen_suv.json"
            git push || (git pull --rebase && git push)
          fi
